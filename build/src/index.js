"use strict";
/** import fs from "fs";
import { Employee, LiteEmployee } from "./types/Employee";

const myFile = fs.readFileSync("myFile.json")

const myJSON: LiteEmployee[] = JSON.parse(myFile.toString())

let employee = myJSON[0]

console.log(employee.name)*/ 
